<%= link_to "New post", new_post_path %>
<% @posts.each do |post| %>
  <div>
  <p> <%= post.message %> - created by <%= post.user_id %> </p>
  
  <%= link_to "Edit Post", edit_post_path(post) %>
  <%= link_to "Delete Post", post, method: :delete, data: {confirm: "Are you sure?"} %>
  </div>
<% end %>

